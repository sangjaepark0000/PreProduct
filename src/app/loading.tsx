export default function Loading() {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "grid",
        placeItems: "center",
        padding: "2rem",
        color: "#33506a"
      }}
    >
      앱 셸을 준비하고 있습니다.
    </main>
  );
}
